# Copy this file to .env and fill in your key to enable the Gemini AI chatbot.
# Without this key the chatbot falls back to the built-in offline rule-based engine.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
